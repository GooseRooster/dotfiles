{{ if not (index . "gaming_enabled" | default false) -}}
.local/bin/game-performance.sh
{{- end }}

{{ if index . "devcontainer_enabled" | default false -}}
.config/ghostty
.config/mpv
.config/environment.d
.config/owl.jpg
.config/tinted-theming
.local/bin/termapp
.local/bin/devcontainer-init
{{- end }}

{{ if index . "wsl_enabled" | default false -}}
.config/ghostty
.config/mpv
.config/environment.d
.config/owl.jpg
.config/tinted-theming
.local/bin/termapp
{{- end }}

{{ if not (index . "wsl_enabled" | default false) -}}
.config/systemd/user/ssh-agent.service
.config/systemd/user/default.target.wants/ssh-agent.service
{{- end }}

{{ if not (index . "podman_alias_enabled" | default false) -}}
.config/systemd/user/sockets.target.wants/podman.socket
{{- end }}

# Repo-management files that live at the source root — never apply these to $HOME.
README.md
LICENSE
extensions.txt
bootstrap.sh
bootstrap-cli.sh
setup-docker-wsl.sh
*.Brewfile

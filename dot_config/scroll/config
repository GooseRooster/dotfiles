### Variables
set $mod Mod4
set $left h
set $right l
set $up k
set $down j

set $term kitty
set $menu rofi -show combi
set $filemanager kitty -e yazi
set $browser zen-browser
set $steam steam
set $music flatpak run io.github.lullabyX.sone
set $ipc noctalia msg

### Output configuration
#
# HDR10 requires the Vulkan renderer: set WLR_RENDERER=vulkan in your environment
# (e.g. via a UWSM env file or wrapper). allow_tearing uses yes/no in scroll, not on/off.
# Run `scrollmsg -t get_outputs` to verify your connector name.
#
output DP-3 {
    resolution 3440x1440@165Hz
    hdr on
    adaptive_sync off
    allow_tearing yes
}
# output DP-1 {
#     resolution 3440x1440@165
#     position 0,0
#     hdr on
#     adaptive_sync on
# }

### Appearance
default_border pixel 2
default_floating_border pixel 2
default_decoration border_radius 0 shadow true shadow_dynamic true shadow_size 2 shadow_blur 20 shadow_offset 10 10 shadow_color #1a1a1aee dim false dim_color #00000040
gaps inner 5
gaps outer 20

workspace_auto_back_and_forth yes
focus_wrapping no

### Scroll layout
layout_default_width 0.5
layout_default_height 1.0
layout_widths [0.33333333 0.5 0.666666667 1.0]
layout_heights [0.33333333 0.5 0.666666667 1.0]
focus_wrapping no
maximize_if_single true

### Animations
animations {
    enabled yes
    default yes 300 var 3 [ 0.215 0.61 0.355 1 ]
    window_open yes 300 var 3 [ 0 0 1 1 ]
    window_move yes 300 var 3 [ 0.215 0.61 0.355 1 ] off 0.05 6 [0 0.6 0.4 0 1 0 0.4 -0.6 1 -0.6]
    window_size yes 300 var 3 [ -0.35 0 0 0.5 ]
    workspace_switch yes 400 var 3 [ 0.215 0.61 0.355 1 ]
    window_fullscreen yes 500 var 3 [ 0.3 0.5 0.4 1 ]
}

### Input
input type:keyboard {
    xkb_layout "us"
}

input type:pointer {
    accel_profile flat
    pointer_accel 0
}

input type:touchpad {
    natural_scroll disabled
    tap enabled
    dwt enabled
}

### Autostart
exec uwsm finalize SWAYSOCK I3SOCK
exec uwsm app -- noctalia
exec uwsm app -- udiskie
exec ~/.config/scroll/scripts/vrr-fullscreen.sh


## Warmup scratchpad-term
exec kitty --app-id=scratchpad-term

### Gestures (3-finger swipe = workspace navigation)
bindgesture swipe:3:right workspace next
bindgesture swipe:3:left workspace prev

### Drag/resize floating windows with mod + mouse
floating_modifier $mod normal


## Scratch pad term
for_window [app_id="scratchpad-term"] {
    floating enable
    resize set 1200 700
    move position center
}

### Key bindings

## App launchers
bindsym $mod+semicolon exec $term
bindsym $mod+e exec $filemanager
bindsym $mod+b exec $browser
bindsym $mod+i exec kitty -e yazi ~/.config
bindsym $mod+space exec $menu
bindsym $mod+g exec $steam
bindsym $mod+m exec $music

## Window management
bindsym $mod+q kill
bindsym $mod+f fullscreen
bindsym $mod+Shift+f floating toggle

## Focus (arrow keys)
bindsym $mod+$left focus left
bindsym $mod+$right focus right
bindsym $mod+$up focus up
bindsym $mod+$down focus down

## Move windows (Alt + arrows)
bindsym $mod+Alt+$left move left
bindsym $mod+Alt+$right move right
bindsym $mod+Alt+$up move up
bindsym $mod+Alt+$down move down

## Move window into adjacent container (Ctrl+Alt+arrows)
# Scroll's `nomode` flag has no scroll equivalent; plain move used.
# Behaviour will differ from Scroll — scroll `move` targets the tree, not a column.
bindsym $mod+Ctrl+Alt+$right move right
bindsym $mod+Ctrl+Alt+$left move left
bindsym $mod+Ctrl+Alt+$up move up
bindsym $mod+Ctrl+Alt+$down move down

## Resize windows (Shift + arrows)
bindsym $mod+Shift+$right resize grow width 20px
bindsym $mod+Shift+$left resize shrink width 20px
bindsym $mod+Shift+$down resize grow height 20px
bindsym $mod+Shift+$up resize shrink height 20px


## Workspaces
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

## Cycle workspaces
bindsym $mod+Ctrl+$down workspace next
bindsym $mod+Ctrl+$up workspace prev
bindsym --whole-window $mod+button5 workspace next
bindsym --whole-window $mod+button4 workspace prev

## Reload config
bindsym $mod+Shift+c reload

# Wrapper script for screenshots/video with gpu-screenrec
bindsym Print         exec ~/.config/scroll/scripts/capture.sh screenshot full
bindsym $mod+Print    exec ~/.config/scroll/scripts/capture.sh screenshot region
bindsym $mod+r        exec ~/.config/scroll/scripts/capture.sh record
bindsym $mod+Shift+r  exec ~/.config/scroll/scripts/capture.sh record region
bindsym $mod+Ctrl+r        exec ~/.config/scroll/scripts/capture.sh record full mic
bindsym $mod+Ctrl+Shift+r  exec ~/.config/scroll/scripts/capture.sh record region mic

## Noctalia IPC
bindsym $mod+comma exec $ipc panel-toggle control-center
##bindsym $mod+space exec $ipc panel-toggle launcher
bindsym $mod+Delete exec $ipc panel-toggle session
bindsym $mod+Escape exec $ipc screen-lock 
bindsym $mod+F1 exec $ipc panel-toggle control-center calendar
bindsym $mod+End exec $ipc settings-toggle
bindsym $mod+F2 exec $ipc panel-toggle control-center system
bindsym $mod+v exec $ipc panel-toggle clipboard

## Media / volume keys (via Noctalia IPC, work on lock screen)
bindsym --locked XF86AudioRaiseVolume exec $ipc volume-up
bindsym --locked XF86AudioLowerVolume exec $ipc volume-down
bindsym --locked XF86AudioMute exec $ipc volume-mute
bindsym --locked XF86AudioMicMute exec $ipc mic-mute
bindsym --locked XF86MonBrightnessUp exec $ipc brightness-up
bindsym --locked XF86MonBrightnessDown exec $ipc brightness-down
bindsym --locked XF86AudioNext exec $ipc media next
bindsym --locked XF86AudioPlay exec $ipc media toggle
bindsym --locked XF86AudioPrev exec $ipc media previous

include /home/gooze/.config/scroll/noctalia

### Variables
set $mod Mod4
set $left Left
set $right Right
set $up Up
set $down Down

set $term kitty
set $filemanager kitty -e yazi
set $browser zen-browser
set $steam steam
set $music flatpak run io.github.lullabyX.sone
set $ipc noctalia msg

### Output configuration
#
# HDR10 requires the Vulkan renderer: set WLR_RENDERER=vulkan in your environment
# (e.g. via a UWSM env file or wrapper). allow_tearing uses yes/no in sway, not on/off.
# Run `swaymsg -t get_outputs` to verify your connector name.
#
output DP-3 {
    resolution 3440x1440@165Hz
    hdr on
    adaptive_sync off
    allow_tearing yes
}
# output DP-1 {
#     resolution 3440x1440@165
#     position 0,0
#     hdr on
#     adaptive_sync on
# }

### Appearance
default_border pixel 2
default_floating_border pixel 2

gaps inner 5
gaps outer 20

workspace_auto_back_and_forth yes
focus_wrapping no

### Input
input type:keyboard {
    xkb_layout "us"
}

input type:pointer {
    accel_profile flat
    pointer_accel 0
}

input type:touchpad {
    natural_scroll disabled
    tap enabled
    dwt enabled
}

### Autostart
exec uwsm finalize SWAYSOCK I3SOCK
exec uwsm app -- noctalia
exec uwsm app -- udiskie
exec ~/.config/sway/scripts/vrr-fullscreen.sh
# autotiling: -l 2 limits split depth to approximate master-stack style.
# exec_always ensures it respawns on config reload.
# Install: yay -S autotiling  (AUR) | apt install autotiling | dnf install autotiling
exec_always autotiling -l 2
## Requires sway-contrib
exec_always /usr/share/sway-contrib/inactive-windows-transparency.py -o 0.85

### Gestures (3-finger swipe = workspace navigation)
bindgesture swipe:3:right workspace next
bindgesture swipe:3:left workspace prev

### Drag/resize floating windows with mod + mouse
floating_modifier $mod normal

### Window rules
for_window [all] inhibit_idle fullscreen
for_window [app_id="scratchpad-term"] move scratchpad, resize set 80ppt 70ppt
exec foot --app-id=scratchpad-term

bindsym $mod+Return exec ~/.config/sway/scripts/scratchpad-term.sh

### Key bindings

## App launchers
bindsym $mod+semicolon exec $term
bindsym $mod+e exec $filemanager
bindsym $mod+b exec $browser
bindsym $mod+i exec kitty -e yazi ~/.config
##bindsym $mod+space exec $menu
bindsym $mod+g exec $steam
bindsym $mod+m exec $music

## Window management
bindsym $mod+q kill
bindsym $mod+f fullscreen
bindsym $mod+Shift+f floating toggle

## Focus (arrow keys)
bindsym $mod+Left focus left
bindsym $mod+Right focus right
bindsym $mod+Up focus up
bindsym $mod+Down focus down

## Move windows (Alt + arrows)
bindsym $mod+Alt+Left move left
bindsym $mod+Alt+Right move right
bindsym $mod+Alt+Up move up
bindsym $mod+Alt+Down move down

## Move window into adjacent container (Ctrl+Alt+arrows)
# Scroll's `nomode` flag has no sway equivalent; plain move used.
# Behaviour will differ from Scroll — sway `move` targets the tree, not a column.
bindsym $mod+Ctrl+Alt+Right move right
bindsym $mod+Ctrl+Alt+Left move left
bindsym $mod+Ctrl+Alt+Up move up
bindsym $mod+Ctrl+Alt+Down move down

## Resize windows (Shift + arrows)
bindsym $mod+Shift+Right resize grow width 20px
bindsym $mod+Shift+Left resize shrink width 20px
bindsym $mod+Shift+Down resize grow height 20px
bindsym $mod+Shift+Up resize shrink height 20px


## Workspaces
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

## Cycle workspaces
bindsym $mod+Ctrl+Right workspace next
bindsym $mod+Ctrl+Left workspace prev
bindsym --whole-window $mod+button5 workspace next
bindsym --whole-window $mod+button4 workspace prev

## Reload config
bindsym $mod+Shift+c reload

## Screenshots (grimshot + swappy)
# swappy still handles annotation and save destination (~/.config/swappy/config).
# grimshot doesn't support stdout, so a temp file is used as the handoff.
bindsym Print exec bash -c 'f=$(mktemp /tmp/shot-XXXXXX.png) && grimshot save area "$f" && swappy -f "$f"'
bindsym $mod+Print exec bash -c 'f=$(mktemp /tmp/shot-XXXXXX.png) && grimshot save output "$f" && swappy -f "$f"'
bindsym $mod+Shift+Print exec bash -c 'f=$(mktemp /tmp/shot-XXXXXX.png) && grimshot save window "$f" && swappy -f "$f"'

## Screen recording (wl-screenrec, 10-bit HEVC for HDR)
# HDR capture works if sway is running with WLR_RENDERER=vulkan and hdr is on.
bindsym $mod+F9 exec bash -c 'wl-screenrec --codec hevc --encode-pixfmt p010 --audio -f ~/Videos/Recordings/$(date +%Y%m%d_%H%M%S).mp4 &'
bindsym $mod+F10 exec pkill -INT wl-screenrec

## Noctalia IPC
bindsym $mod+comma exec $ipc panel-toggle control-center
bindsym $mod+space exec $ipc panel-toggle launcher
bindsym $mod+Delete exec $ipc panel-toggle session
bindsym $mod+l exec $ipc screen-lock
bindsym $mod+Shift+l exec bash -c 'noctalia msg screen-lock && systemctl suspend'
bindsym $mod+F1 exec $ipc panel-toggle control-center calendar
bindsym $mod+End exec $ipc settings-toggle
bindsym $mod+F2 exec $ipc panel-toggle control-center system
bindsym $mod+v exec $ipc panel-toggle clipboard

## Media / volume keys (via Noctalia IPC, work on lock screen)
bindsym --locked XF86AudioRaiseVolume exec $ipc volume-up
bindsym --locked XF86AudioLowerVolume exec $ipc volume-down
bindsym --locked XF86AudioMute exec $ipc volume-mute
bindsym --locked XF86AudioMicMute exec $ipc mic-mute
bindsym --locked XF86MonBrightnessUp exec $ipc brightness-up
bindsym --locked XF86MonBrightnessDown exec $ipc brightness-down
bindsym --locked XF86AudioNext exec $ipc media next
bindsym --locked XF86AudioPlay exec $ipc media toggle
bindsym --locked XF86AudioPrev exec $ipc media previous

include /home/gooze/.config/sway/noctalia

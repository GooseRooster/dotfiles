-- nvix — a self-rolled, distro-free Neovim config.
-- Run isolated via `NVIM_APPNAME=nvix nvim` (or the `nvix` shell command).
-- See AGENTS.md for the map; PROJECT.md for build status.
require("config.lazy")
